<?php
// Read-only, aggregate-only WP-CLI helper. Never prints identifiers or rows.
if (!defined('WP_CLI') || !WP_CLI) { exit(1); }
global $wpdb;
$die=function(){WP_CLI::error('Synthetic state query failed.',false);exit(1);};
$table=function($s)use($wpdb){return $wpdb->prefix.'nmkr_'.$s;};
$count=function($sql)use($wpdb,$die){$wpdb->last_error='';$v=$wpdb->get_var($sql);if($wpdb->last_error!==''||$v===null)$die();return(int)$v;};
$row=function($sql)use($wpdb,$die){$wpdb->last_error='';$v=$wpdb->get_row($sql,ARRAY_A);if($wpdb->last_error!=='')$die();return is_array($v)?$v:array();};
$truthy=function($v){if(is_bool($v))return$v;if(is_numeric($v))return(float)$v>0;if(is_string($v))return in_array(strtolower(trim($v)),array('1','true','yes','on','running','in_progress','processing','processing_projects','processing_tokens','initializing','pending'),true);return!empty($v);};
$active=array('initializing','finalizing','processing','processing_projects','processing_tokens','in_progress','running','pending','active','started');
$terminal=array('completed','success','failed','error','stopped','cancelled','aborted');
$sql_list=function($v){return"'".implode("','",array_map('esc_sql',$v))."'";};
$sync_data=get_option('nmkr_sync_data',array());$sync_class='absent';
if(is_array($sync_data)&&$sync_data){$sync_class='unknown';foreach(array('status','sync_status','state')as$k){$s=strtolower(trim((string)($sync_data[$k]??'')));if(in_array($s,$active,true)){$sync_class='active';break;}if(in_array($s,$terminal,true))$sync_class='terminal';}foreach(array('in_progress','is_running','running','pending','active')as$k)if(($sync_data[$k]??null)===true)$sync_class='active';if(array_key_exists('completed',$sync_data)&&$sync_data['completed']===false)$sync_class='active';}
$projects=$table('projects');$tokens=$table('tokens');$details=$table('token_details');$history=$table('sync_stats');$metrics=$table('sync_metrics');
$expected='';$receipt=getenv('NMKR_SYNTHETIC_RUN_RECEIPT');if(is_string($receipt)&&$receipt!==''&&is_file($receipt)){$raw=json_decode((string)file_get_contents($receipt),true);if(!is_array($raw)||!preg_match('/^[0-9a-f-]{36}$/i',(string)($raw['run_id']??'')))$die();$expected=(string)$raw['run_id'];}
$history_rows=$expected===''?array():$wpdb->get_results($wpdb->prepare("SELECT id,status,items_processed,items_successful,items_failed,end_time,error_message FROM `$history` WHERE BINARY run_id=%s",$expected),ARRAY_A);if($wpdb->last_error!=='')$die();
$exact_history_count=count((array)$history_rows);$h=$exact_history_count===1?$history_rows[0]:array();$history_id=(int)($h['id']??0);
$receipt_row=$history_id>0?$row($wpdb->prepare("SELECT option_value FROM {$wpdb->options} WHERE option_name=%s",'nmkr_sync_finalizing_'.$history_id)):array();$metric=array();$metric_id=0;
if(isset($receipt_row['option_value'])){$mr=maybe_unserialize($receipt_row['option_value']);if(is_array($mr)&&(int)($mr['sync_stats_id']??0)===$history_id)$metric_id=(int)($mr['metrics_id']??0);}
if($metric_id>0)$metric=$row($wpdb->prepare("SELECT id,last_sync_time,total_projects,total_tokens,total_sync_duration,total_api_time,average_response_time,api_requests,memory_usage FROM `$metrics` WHERE id=%d",$metric_id));
$ctx=hash_init('sha256');$terminal_rows=$wpdb->get_results("SELECT id,run_id,sync_type,start_time,end_time,status,items_processed,items_successful,items_failed,updated_at FROM `$history` WHERE BINARY status IN (".$sql_list($terminal).")".($expected!==''?$wpdb->prepare(' AND BINARY run_id<>%s',$expected):'').' ORDER BY id',ARRAY_A);if($wpdb->last_error!=='')$die();foreach((array)$terminal_rows as$r)hash_update($ctx,hash('sha256',wp_json_encode($r))."\n");
$hooks=array('nmkr_execute_sync_background','nmkr_process_batch_hook','nmkr_sync_cron_hook','nmkr_install_sync_cron_hook','nmkr_resume_sync_finalization');$cron=array_fill_keys($hooks,0);$cron_inspectable=true;$cron_raw=get_option('cron',array());if(!is_array($cron_raw)||!isset($cron_raw['version'])||(int)$cron_raw['version']!==2)$cron_inspectable=false;else foreach($cron_raw as$ts=>$events){if($ts==='version')continue;if(!ctype_digit((string)$ts)||!is_array($events)){$cron_inspectable=false;break;}foreach($hooks as$hook)if(isset($events[$hook]))$cron[$hook]+=count($events[$hook]);}
$provider=json_decode((string)@file_get_contents((string)getenv('NMKR_SYNTHETIC_STATE_FILE')),true);
$chains=array('cardano_only'=>$count("SELECT COUNT(*) FROM `$projects` WHERE project_uid LIKE 'synthetic-project-%' AND blockchains='[\"Cardano\"]' AND (solana_project_details IS NULL OR solana_project_details='')"),'solana_only'=>$count("SELECT COUNT(*) FROM `$projects` WHERE project_uid LIKE 'synthetic-project-%' AND blockchains='[\"Solana\"]' AND solana_project_details<>''"),'dual_chain'=>$count("SELECT COUNT(*) FROM `$projects` WHERE project_uid LIKE 'synthetic-project-%' AND blockchains='[\"Cardano\",\"Solana\"]' AND solana_project_details<>''"));
$out=array('schema_version'=>3,'project_count'=>$count("SELECT COUNT(*) FROM `$projects` WHERE project_uid LIKE 'synthetic-project-%'"),'token_count'=>$count("SELECT COUNT(*) FROM `$tokens` WHERE token_uid LIKE 'synthetic-project-%-token-%'"),'token_detail_count'=>$count("SELECT COUNT(*) FROM `$details` d INNER JOIN `$tokens` t ON t.token_uid=d.token_uid WHERE t.token_uid LIKE 'synthetic-project-%-token-%'"),'chain_classifications'=>$chains,'duplicate_project_count'=>$count("SELECT COUNT(*) FROM (SELECT project_uid FROM `$projects` WHERE project_uid LIKE 'synthetic-project-%' GROUP BY project_uid HAVING COUNT(*)>1)x"),'duplicate_token_count'=>$count("SELECT COUNT(*) FROM (SELECT token_uid FROM `$tokens` WHERE token_uid LIKE 'synthetic-project-%-token-%' GROUP BY token_uid HAVING COUNT(*)>1)x"),'orphan_token_count'=>$count("SELECT COUNT(*) FROM `$tokens`t LEFT JOIN `$projects`p ON p.project_uid=t.project_uid WHERE t.token_uid LIKE 'synthetic-project-%-token-%' AND p.project_uid IS NULL"),'orphan_detail_count'=>$count("SELECT COUNT(*) FROM `$details`d LEFT JOIN `$tokens`t ON t.token_uid=d.token_uid WHERE d.token_uid LIKE 'synthetic-project-%-token-%' AND t.token_uid IS NULL"),'history_count'=>$count("SELECT COUNT(*) FROM `$history`"),'metrics_count'=>$count("SELECT COUNT(*) FROM `$metrics`"),'terminal_history_fingerprint'=>hash_final($ctx),'exact_history_count'=>$exact_history_count,'exact_history'=>array('status'=>(string)($h['status']??''),'items_processed'=>(int)($h['items_processed']??0),'items_successful'=>(int)($h['items_successful']??0),'items_failed'=>(int)($h['items_failed']??0),'ended'=>!empty($h['end_time']),'error_free'=>empty($h['error_message'])),'exact_metrics_count'=>$metric_id>0&&$metric?1:0,'exact_metrics'=>array('total_projects'=>(int)($metric['total_projects']??0),'total_tokens'=>(int)($metric['total_tokens']??0),'total_sync_duration'=>(float)($metric['total_sync_duration']??0),'total_api_time'=>(float)($metric['total_api_time']??0),'average_response_time'=>(float)($metric['average_response_time']??0),'api_requests'=>(int)($metric['api_requests']??0),'memory_usage'=>(float)($metric['memory_usage']??0),'timestamp_match'=>$metric&&((string)$metric['last_sync_time']===(string)($h['end_time']??''))),'active_history_count'=>$count("SELECT COUNT(*) FROM `$history` WHERE status IS NULL OR BINARY status IN (".$sql_list($active).")"),'owner_present'=>$truthy(get_option('nmkr_sync_owner',false)),'option_active_marker_count'=>(int)$truthy(get_option('nmkr_sync_in_progress',false))+(int)$truthy(get_option('nmkr_sync_near_completion',false))+(int)$truthy(get_option('nmkr_sync_status',false)),'transient_active_marker_count'=>(int)$truthy(get_transient('nmkr_sync_in_progress'))+(int)$truthy(get_transient('nmkr_sync_progress'))+(int)$truthy(get_transient('nmkr_sync_status'))+(int)$truthy(get_transient('nmkr_stale_recovery_running')),'stale_recovery_marker_count'=>(int)$truthy(get_option('nmkr_stale_recovery_running',false))+(int)$truthy(get_transient('nmkr_stale_recovery_running')),'worker_evidence_count'=>(int)$truthy(get_option('nmkr_sync_heartbeat',false))+(int)$truthy(get_option('nmkr_sync_worker_started_at',false))+(int)$truthy(get_option('nmkr_sync_worker_lock',false))+(int)$truthy(get_transient('nmkr_sync_worker_lock')),'live_metrics'=>(bool)get_transient('nmkr_current_sync_stats_live'),'finalization_resume_marker_count'=>$count($wpdb->prepare("SELECT COUNT(*) FROM {$wpdb->options} WHERE option_name LIKE %s",$wpdb->esc_like('nmkr_sync_finalization_resume_').'%'))+$cron['nmkr_resume_sync_finalization'],'sync_data_classification'=>$sync_class,'cron_inspectable'=>$cron_inspectable,'cron'=>$cron,'provider_counters'=>is_array($provider)&&isset($provider['counters'])?$provider['counters']:null);
WP_CLI::line(wp_json_encode($out));
